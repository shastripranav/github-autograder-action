"""Find the k most frequent elements in a list.

This is the student submission file. Replace the function body with your solution.
"""


def top_k_frequent(nums: list[int], k: int) -> list[int]:
    """Return the k most frequently occurring elements in nums.

    Args:
        nums: List of integers (may contain duplicates).
        k: Number of top-frequent elements to return.

    Returns:
        List of the k most frequent elements in any order.

    Examples:
        >>> top_k_frequent([1, 1, 1, 2, 2, 3], 2)
        [1, 2]
        >>> top_k_frequent([1], 1)
        [1]
    """
    # TODO: Implement your solution here
    pass
